{
  "name": "multi-player",
  "version": "alpha 0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite apps/client --port 5173",
    "dev:client": "vite apps/client --port 5173",
    "dev:server": "node apps/server/src/index.js",
    "dev:all": "concurrently \"npm:dev:server\" \"npm:dev:client\"",
    "build": "vite build apps/client",
    "preview": "vite preview apps/client --port 5173"
  },
  "devDependencies": {
    "concurrently": "^8.0.1",
    "cross-env": "^7.0.3",
    "vite": "^8.0.12"
  }
}
